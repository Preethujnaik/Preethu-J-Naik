Q1. 
 
Sample Input: val[] = {6, 7, 1, 3, 8, 2, 5} 
Sample Output: 20 
ANS:>>> 
def max_steal(val): 
    n = len(val) 
    if n == 0: 
        return 0 
    if n == 1: 
        return val[0] 
    dp = [0] * n 
    dp[0] = val[0] 
    dp[1] = max(val[0], val[1]) 
     
    for i in range(2, n): 
        dp[i] = max(val[i] + dp[i-2], dp[i-1]) 
    return dp[-1] 
val = [6, 7, 1, 3, 8, 2, 5] 
print("Maximum stolen value:", max_steal(val)) 
 
 
-----------------------------------------------------------
 
Q2. 

#include <stdio.h> 
 
int main() { 
    int i, j; 
 
    for(i = 1; i <= 5; i++) {          
        for(j = 1; j <= 5; j++) {      
            if(j == 5) { 
                if(i % 2 == 0) { 
                    printf("%d", i + 2);  
                } else { 
                    printf("%d", i + 1);  
                } 
            } else { 
                if(i % 2 == 0) { 
                    printf("%d", i);  
                } else { 
                    printf("%d", i);  
                } 
            } 
        } 
        printf("\n");  
    } 
 
    return 0; 
} 
 
------------------------------------------------------------------

Q3.
 
 
def is_disarium(num): 
    temp = num 
    count = len(str(num)) 
    total = 0 
    for index, digit in enumerate(str(num), start=1): 
        total += int(digit) ** index 
    return total == temp 
 
def first_n_disarium(n): 
    print(f"First {n} Disarium numbers are:") 
    num = 0 
    count = 0 
    while count < n: 
        if is_disarium(num): 
            print(num, end=" ") 
            count += 1 
        num += 1 
    print() 
 
def disarium_in_range(start, end): 
    print(f"Disarium numbers between {start} and {end} are:") 
    for num in range(start, end + 1): 
        if is_disarium(num): 
            print(num, end=" ") 
    print() 
 
n = int(input("Enter how many Disarium numbers you want: ")) 
first_n_disarium(n) 
 
start = int(input("Enter start of range: ")) 
end = int(input("Enter end of range: ")) 
disarium_in_range(start, end) 